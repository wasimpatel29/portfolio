version: 0.2

phases:
  install:
    runtime-versions:
        node: 10
  build:
    commands:
      - node --version
artifacts:
  files:
    - index.html
    - resume.pdf
    - favicon.ico
    - styles/*
    - images/*